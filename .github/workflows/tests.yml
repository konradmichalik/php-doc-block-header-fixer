name: Tests
on:
  push:
    branches:
      - '**'

jobs:
  tests:
    uses: konradmichalik/reusable-github-actions/.github/workflows/tests-php.yml@7e9510ad26b6bc039a13aa4bd9fa44a1a0b723bc # main
    with:
      php-versions: '["8.2", "8.3", "8.4", "8.5"]'
      dependencies: '["highest", "lowest"]'
